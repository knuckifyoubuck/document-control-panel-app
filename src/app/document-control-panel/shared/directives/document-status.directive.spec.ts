import { DocumentStatusDirective } from '../document-status.directive';

describe('DocumentStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new DocumentStatusDirective();
    expect(directive).toBeTruthy();
  });
});
